# M0.4 architecture selection evidence

Task input revision: `7714dff0ff0eeb3ce35c094fb1e29198f60dd9bd`

Selected mode: `LIMITED_MODE`

## Acceptance mapping

- Exactly one supported mode is selected in `state/architecture-decision.md`.
- Observed protocol facts are listed without extending the missing A→B evidence.
- `GO_PATH_A`, `GO_PATH_A_REHYDRATE`, `GO_PATH_B`, and `STOP_UNSAFE` are explicitly evaluated.
- Residual protocol, rehydration, tool-side-effect, auxiliary-endpoint, and product-wording risks are
  recorded.
- M0.3 remains blocked and the user-directed development deferral is explicit.
- M1 implementation is constrained to non-transparent LIMITED_MODE behavior.

## Validation commands and results

```text
jq -e '<assert M0.4 in_progress, LIMITED_MODE, null output revision>' state/task-status.json
# true; exit 0

ruby -ryaml -e '<validate M0.3 blocked, M0.4 in_progress, M1.1 pending, and EX-DEFER-M0.3-CROSS>'
# DAG exception and task states valid; exit 0

test "$(rg -c '^Mode: LIMITED_MODE$' state/architecture-decision.md)" = "1"
# exit 0

git diff --check
# exit 0

rg '<credential values, known token shapes, or email>' <M0.4 changed files>
# no matches; exit 1 interpreted as pass
```
