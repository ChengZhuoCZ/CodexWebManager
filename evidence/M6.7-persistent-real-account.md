# M6.7 persistent real-account evidence

## Task record

- Repository: `ChengZhuoCZ/CodexWebManager`
- Branch: `codex/m6-7-persistent-real-account`
- Input commit: `25ad185a85526c96ef6aad2b8b5f04f8ad39d958`
- Dependencies: `M6.6`, `G-SECRETS`
- Implementation mode: `clean-room`
- Started: `2026-07-28T19:48:51+08:00`
- Completed: `2026-07-28T20:06:41+08:00`

M6.7 was limited to persisting one account that the user had already authorized. No account was
added or switched during this task, no model request was sent, and no cross-account continuity was
tested or claimed.

## Minimum experiment

The initial production browser identity was a router-managed local marker. A private isolated App
Server probe then read the already authorized real account credential without invoking a model. Its
sanitized result was:

```text
account_present=true
account_type=chatgpt
model_request_sent=false
```

The probe emitted no account identifier, email, token, OAuth callback, device code, credential
digest, or complete authentication response.

## Production change

The same already authorized credential was installed into these persistent, server-only locations:

```text
/etc/credstore/codex-account-router.auth.primary
/etc/codex-account-router/credentials/app-server-auth.json
/var/lib/codex-web/auth.json
```

Only file metadata and byte equality were checked. The router and App Server copies are root-owned
mode `0600`; the codex-web copy is owned by `codex` and mode `0600`. All three copies were equal.
The App Server and codex-web services were restarted independently. All diagnostic credential
copies, the isolated probe home, and the uploaded probe script were removed afterward.

## Production verification

The browser IPC probe and fresh-page visual checks were run before and after a second service
restart:

| Check | Before restart | After restart |
| --- | --- | --- |
| HTTP root | `200` | `200` |
| Browser session cookie | present | present |
| Browser IPC WebSocket | open | open |
| App Server account | present, `chatgpt` | present, `chatgpt` |
| ChatGPT sign-in prompt | absent | absent |
| New task control | present | present |
| Composer | present | present |
| Settings control | present | present |
| `codex-app-server.service` | active | active |
| `codex-web.service` | active | active |

This verifies persistence across service restarts and fresh browser sessions. A host reboot was not
run, so reboot persistence remains an operational residual risk rather than a tested claim.

## Repository verification

Focused documentation and systemd contracts:

```sh
/Users/chengzhuo/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node \
  --test \
  packages/account-router/test/release-docs-contract.test.mjs \
  packages/account-router/test/security-audit-contract.test.mjs \
  packages/account-router/test/systemd-units.test.mjs
```

Result: `18` passed, `0` failed, `0` skipped.

Final fail-closed security audit, using the clean pinned codex-web fixture:

```sh
M6_3_CODEX_WEB_ROOT=/private/tmp/m67-pristine-codex-web \
  /Users/chengzhuo/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node \
  packages/account-router/scripts/run-security-audit.mjs \
  --summary /private/tmp/codex-m6-7-security/summary.json
```

Result: `78` passed, `0` failed, `0` skipped; `291` repository files scanned with `0` secret
findings. The referenced strict UI baseline remained unchanged: `476` passed, `0` failed,
`0` skipped.

An initial sandboxed audit attempt was not qualified because the sandbox denied loopback listener
creation. A second diagnostic attempt without the pinned codex-web fixture completed the functional
tests but skipped `13` fixture-dependent tests. Neither result was accepted as evidence; the final
command above ran outside that listener restriction with the clean pinned fixture and no skips.

## Residual risks

- Every Tailnet principal allowed to reach port `8214` can act through this real account and the
  configured workspaces.
- A live account credential now exists in three private server files, increasing the impact of a
  server or service-user compromise.
- Provider expiry or revocation requires authorization again; rotation and revocation must update
  or remove all three copies together.
- Host reboot persistence was not tested.
- Account switching, quota-triggered failover, active model execution, and cross-account continuity
  remain outside M6.7.
