# M6.4 release and operator documentation evidence

## Task record

- Branch: `codex/m6-4-release-operator-docs`
- Input commit: `a2b970f`
- Validated implementation: `c20087e69714dc1f13c56bfabb6497c6d3f4c070`
- Dependencies: M6.3 (`done`) and M6.2 (`rejected` under `EX-SKIP-M6.2-SOAK`)
- Mode: clean-room `LIMITED_MODE`

## Delivered documentation

`docs/operator-guide.md` is the unified runbook for prerequisites, verified installation, public
metadata/private credential configuration, loopback listeners, SSH-over-Tailscale access,
monitoring, credential rotation, non-secret backup, upgrade, rollback, and incident response.

`docs/release-status.md` separates deployability from release qualification. It records M6.2 as
rejected, the 24-hour soak as incomplete, real-account switching as deferred, and seamless or
in-flight continuity as unclaimed. `DEPLOYMENT.md` and `SECURITY.md` link to both documents.

## Verification

The failure-first contract initially reported three missing-document failures. After implementation:

- documentation and matrix contracts: 7/7 passed;
- security audit: passed with zero findings and zero focused-test skips;
- full local suite: 208 total, 201 passed, 0 failed, 7 pinned-checkout skips;
- GitHub run `30263647985`: Node 22 and Node 24 strict matrices passed with zero skips;
- security run `30263648033`, Linux run `30263648074`, systemd run `30263647991`, and
  upgrade/rollback run `30263647979`: passed.

No real account was configured or switched. The documentation does not waive M6.2 or any deferred
real-account gate and does not declare this revision production-release qualified.

## Residual release blockers

- The required M6.2 24-hour soak is not qualified.
- Real A-to-B account protocol continuity and approved product E2E remain deferred.
- Any reverse proxy or tailnet-wide web publication requires a deployment-specific authentication
  and security review; the checked-in services remain loopback/Unix-socket only.
