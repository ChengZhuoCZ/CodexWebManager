# M0.3 blocker and deferred verification

Status: blocked

Recorded: 2026-07-16 +08:00

## Blocking condition

The current device has one user-authorized Codex `CODEX_HOME`. A second distinct identity is
required to execute the real A→B continuity rows without fabricating evidence or treating a mock as
a real-account E2E.

## User-directed deferral

On 2026-07-16 the user instructed the project to defer account-switch testing and complete
development first. Development may therefore proceed under the M0.4 `LIMITED_MODE` decision.

This deferral does not:

- mark M0.3 done;
- prove cross-account response ID portability;
- authorize a seamless-continuation claim;
- waive M3.5/M4.1 real account-switch acceptance;
- waive the final release/security gate.

## Work allowed while deferred

- headless service foundations that start with zero accounts;
- account metadata and pluggable secret-provider boundaries;
- deterministic scheduling, cooldowns, circuit breakers, and bounded retry logic;
- strict proxy allowlists and SSE semantic-boundary enforcement;
- explicit new-session transcript rehydration for LIMITED_MODE;
- sanitized admin/event APIs, optional UI status, systemd packaging, and automated mock/integration
  coverage.

## Work that remains unverified

- real A→B calls with `previous_response_id` present and absent;
- real A→B fallback before the first semantic event;
- account-specific auxiliary endpoint compatibility;
- same-page account-switch E2E through codex-web;
- any product claim that an existing upstream response chain remains intact after switching.

## Unblock procedure

Provide two distinct user-authorized `CODEX_HOME` directories, run the documented cross-account M0.3
scenario groups, scan the resulting evidence for secrets, and update the continuity matrix before
changing M0.3 from `blocked`.
