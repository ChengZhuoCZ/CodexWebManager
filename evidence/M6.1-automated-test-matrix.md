# M6.1 automated test matrix evidence

## Task record

- Repository: `ChengZhuoCZ/CodexWebManager`
- Branch: `codex/m6-1-automated-test-matrix`
- Input commit: `af53caf6e0658b8d0b85da48e13dcb1886f3f350`
- Dependencies: M4.3 (`done`), M5.3 (`done`)
- Implementation mode: `clean-room`
- Architecture mode: `LIMITED_MODE`
- Expected output: an auditable unit/integration/E2E matrix, explicit failure-injection
  coverage, a native CI workflow, and sanitized test evidence.

M6.1 uses fixtures and pinned local checkouts. It does not configure or switch a real account,
does not waive the deferred real-account gates, and makes no seamless account-continuity claim.

## Initial inspection

The package starts with 186 Node test rows and separate release/systemd workflows. There is no
single checked-in matrix that classifies required suites, proves each required fault is exercised,
fails closed when a required row disappears, or emits one sanitized machine-readable summary.

Test-first commands and final evidence will be appended as the task proceeds.
