# M6.2 24-hour soak evidence

## Task record

- Repository: `ChengZhuoCZ/CodexWebManager`
- Branch: `codex/m6-2-24h-soak`
- Input commit: `dcb44b621af8e962799fb6e9643dbc54375dc9e3`
- Dependency: M6.1 (`done`)
- Implementation mode: `clean-room`
- Architecture mode: `LIMITED_MODE`
- Expected output: a fail-closed fixture soak harness, resource/retry checkpoints, restart and
  new-traffic recovery drills, and one complete wall-clock 24-hour Linux run.

M6.2 does not configure a real account. A restart drill may prove that the service and new fixture
traffic recover; it must not be described as an in-flight computation resuming in place.

## Test-first implementation

The first contract run failed because the soak runner did not exist. The implemented runner now:

- refuses any duration override in acceptance mode and fixes it to 86,400,000 milliseconds;
- requires private absolute summary and checkpoint paths;
- uses three fixture account aliases and no real credential provider;
- injects quota, generic 429, authentication, network, 5xx, malformed pre-semantic SSE, and
  post-semantic disconnect behavior;
- caps attempts at three, total request time at five seconds, and concurrency at four;
- samples RSS, Linux file descriptors, concurrency, attempt counts, and process health;
- flushes sanitized checkpoints to disk and fails closed on an incomplete or unhealthy run;
- drains traffic before every worker replacement and verifies only a new fixture request afterward.

## Local preflight

- `<bundled-node> --test test/soak-contract.test.mjs`: 4/4 passed.
- `<bundled-node> --test test/test-matrix-contract.test.mjs`: 4/4 passed.
- module syntax check across bin, source, scripts, fixtures, and tests: passed.
- `<bundled-node> --test`: 200 total, 193 passed, 0 failed, 7 pinned-checkout rows skipped in
  the local environment.
- five-second fixture smoke: 194 client requests, zero request errors, two recovered worker
  replacements, bounded retries/concurrency/resources, and all configured fault categories
  exercised.

The sanitized local summary is `evidence/M6.2/local-test-summary.json`. It explicitly records that
no real account or account switch was configured and that neither in-flight computation recovery
nor seamless account continuity is claimed. It also records `qualified_24_hour_soak: false`.

## Linux preflight

GitHub Actions run `30254103792` validated source commit
`33892581ed3d730333d4136211c749a46f049942` on Linux:

- Node 22.23.1: 200/200 passed, zero failures, zero skips.
- Node 24.18.0: 200/200 passed, zero failures, zero skips.
- The E2E suite included the five-second soak contract and its two process replacement drills.

The same source commit also passed the x64/arm64 Linux headless build, systemd least-privilege, and
upgrade/rollback workflows. The sanitized record is
`evidence/M6.2/linux-preflight-summary.json`. These short tests do not satisfy the 24-hour gate.

An immutable `git archive` deployment package was built from the same source commit. Its sanitized
manifest and SHA-256 are recorded in `evidence/M6.2/deployment-package.json`; it contains no real
account configuration or credentials.

## Pending acceptance evidence

The real wall-clock 24-hour Linux run has not yet completed. M6.2 remains `in_progress`; no
acceptance claim is made until the Linux summary and minute checkpoints are reviewed. A bounded
password login through the tailnet TCP proxy succeeded and confirmed Linux x64, Node 22.23.1,
systemd 255, and sufficient disk space. The target went offline before the archive upload began, so
no remote deployment or soak process is claimed. No address, user name, or credential is stored in
repository evidence.

On the next retry, the configured exit node was confirmed selected and online. The target appeared
online and active in the coordination state but had no current endpoint; tailnet data probes,
proxied SSH, and direct SSH all timed out before authentication. This is recorded as a target-side
Tailscale data-path blocker, not an authentication failure.

The data path later recovered. The uploaded archive matched its recorded SHA-256 and source commit,
and its runner and worker passed the server's Node syntax checks. A five-second Linux x64
Node 22.23.1 smoke completed 198 client requests with zero request errors, two recovered
new-traffic process replacement drills, and bounded RSS, file descriptors, attempts, and
concurrency. Its full sanitized summary is
`evidence/M6.2/linux-host-smoke-summary.json`; it correctly records
`qualified_24_hour_soak: false`.

The fixed acceptance run started at `2026-07-27T11:04:20Z` as the transient systemd unit
`codex-router-soak-m6-2.service`. The service runs as the unprivileged deployment user with a
512 MiB memory limit, 64-task limit, 512-file-descriptor limit, read-only system protection, no new
privileges, private devices and temporary directory, and write access only to its private evidence
directory. At the first minute checkpoint it had processed 236 client requests with zero errors,
zero unexpected exits, RSS of 68,161,536 bytes, 31 file descriptors, and maximum client
concurrency four.

The expected earliest wall-clock completion is `2026-07-28T11:04:20Z`. M6.2 remains
`in_progress`; these startup observations do not constitute the 24-hour acceptance result.
