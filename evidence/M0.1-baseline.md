# M0.1 — Pin repositories and reproduce baseline

Status: `done`

Recorded at: `2026-07-16T00:06:13+08:00`

Implementation mode: `clean-room`

## Scope and start record

- Workspace repository: `https://github.com/ChengZhuoCZ/CodexWebManager.git`
- Workspace branch: `main`
- Workspace input revision: none; the repository had no `HEAD` commit when M0.1 started.
- Dependencies: none.
- Expected outputs: `evidence/M0.1-baseline.md`, `state/repositories.json`, and raw command logs under `evidence/M0.1/`.
- Production behavior changes: none. Both upstream repositories were checked out only in the ephemeral `/tmp/codex-web-auto-account-switch-m0.1/` directory.
- Clean-room boundary: CodexManager inspection was limited to version/license metadata, public README instructions, builds, and test behavior. No implementation was copied into this repository.

## Exact pins

| Component | URL or executable | Exact revision/version | Verification |
|---|---|---|---|
| codex-web | `https://github.com/0xcaff/codex-web.git` | `888692f7d885118c6a92bbaf60cf2121f5947adf` | Checkout and expected audit SHA match; tree `ff7211b4f2d5ad804544a8d4b0aeb29158a1f220` |
| CodexManager | `https://github.com/meitianwang/CodexManager.git` | `9f9a67df44354da37a8b4ba2bb423a75473b56b6` | Checkout and expected audit SHA match; tree `bbc2b7633e23c111e994822121f55a1ce8527e98` |
| CodexManager relevant history | same repository | `213e848097ee93cd8f24c7b0892400eb995300b6` | Commit object exists; metadata only |
| Codex CLI / App Server | `/Applications/ChatGPT.app/Contents/Resources/codex` | `codex-cli 0.144.2` | SHA-256 `4a9e84ad1c9622e8f6a50dafd75710eaee4af79dc5bcf8bafd4471763255fe18` |
| codex-web prepared app asset | upstream prepare script | `26.707.30751` | Fixed asset downloaded and prepared successfully by `npm ci` |

The Codex App Server is a subcommand of the same pinned Codex CLI executable and exposes no separate version flag.

## Validation environment

- macOS `27.0` build `26A5378n`, arm64
- Git `2.54.0 (Apple Git-157)`
- Node.js `24.14.0`
- npm `11.18.0`
- pnpm `10.34.5`

CodexManager documents Node.js 22+ and pnpm 10.x. Node 24 meets the stated minimum, while the version difference from its Node 22 CI remains a recorded risk.

## Checkout and build commands

### codex-web

```bash
git clone --filter=blob:none --no-checkout https://github.com/0xcaff/codex-web.git /tmp/codex-web-auto-account-switch-m0.1/codex-web
git checkout --detach 888692f7d885118c6a92bbaf60cf2121f5947adf
npm ci
npm run build:browser
npm run build:server
git diff --exit-code
git diff --cached --exit-code
```

Results:

- `npm ci`: passed. Its declared `prepare` lifecycle downloaded asset `26.707.30751`, applied the upstream patches, built the browser bundle, and ran server TypeScript compilation.
- `npm run build:browser`: passed; 67 modules transformed.
- `npm run build:server`: passed; this is the available server TypeScript check.
- No `test` or standalone `typecheck` script exists in the pinned `package.json`, and no repository test/spec files were found.
- Tracked and staged diffs remained empty after validation. TypeScript emitted only untracked files inside the ephemeral checkout.
- npm reported 8 dependency audit findings: 3 moderate and 5 high.

Raw outputs:

- `evidence/M0.1/codex-web-build-browser.log`
- `evidence/M0.1/codex-web-build-server.log`

### CodexManager

```bash
git clone --filter=blob:none --no-checkout https://github.com/meitianwang/CodexManager.git /tmp/codex-web-auto-account-switch-m0.1/CodexManager
git checkout --detach 9f9a67df44354da37a8b4ba2bb423a75473b56b6
cd apps/desktop
pnpm install --frozen-lockfile
pnpm run typecheck
pnpm test
pnpm run build
git diff --exit-code
git diff --cached --exit-code
```

Results:

- Initial install with the host pnpm `11.7.0`: failed with `ERR_PNPM_IGNORED_BUILDS` because pnpm 11 no longer reads the package-level `pnpm.onlyBuiltDependencies` field.
- Reinstall with upstream-compatible pnpm `10.34.5`: passed using the frozen lockfile.
- `pnpm run typecheck`: passed.
- First sandboxed `pnpm test`: failed because the sandbox denied loopback listeners (`listen EPERM 127.0.0.1`) and blocked the Electron download; 14/18 files and 142/206 tests passed.
- First unsandboxed retry: 17/18 files passed and 235 tests passed; one suite failed because parallel Electron extraction encountered a pre-existing symlink.
- After cleaning only the generated temporary Electron `dist` directory and running Electron's declared installer once, the second unsandboxed retry passed: 18/18 files and 236/236 tests.
- `pnpm run build`: passed, including main-process TypeScript compilation and renderer production build.
- Tracked and staged diffs remained empty. pnpm generated an untracked `apps/desktop/pnpm-workspace.yaml` only inside the ephemeral checkout.

Raw outputs:

- `evidence/M0.1/codexmanager-typecheck.log`
- `evidence/M0.1/codexmanager-test.log` — sandboxed environmental failure; Vitest source-code frames mechanically removed for the clean-room license boundary
- `evidence/M0.1/codexmanager-test-unsandboxed-attempt1.log` — Electron extraction environmental failure
- `evidence/M0.1/codexmanager-test-unsandboxed-attempt2.log` — final passing run
- `evidence/M0.1/codexmanager-build.log`

## Secret and source-change checks

Credential-shape scan:

```bash
rg -n -i '(authorization:[[:space:]]*(bearer|basic)[[:space:]]+[^[:space:]]+|cookie:[[:space:]]*[^[:space:]]+|sk-[A-Za-z0-9_-]{16,}|eyJ[A-Za-z0-9_-]{16,}\.[A-Za-z0-9_-]{16,}\.[A-Za-z0-9_-]{16,})' evidence/M0.1 --glob '*.log' --glob '!secret-scan.log'
```

Result: exit code `1`, empty output, meaning no matching credential-shaped value was found. See `evidence/M0.1/secret-scan.log`.

For both upstream checkouts, `git diff --exit-code` and `git diff --cached --exit-code` returned `0`. No upstream tracked source was changed.

## Residual risks

1. M0.2 and M0.3 have not run. No claim is made that account switching preserves an existing conversation, `previous_response_id`, cache state, or in-flight SSE semantics.
2. Validation ran on macOS arm64 with Node.js 24.14.0, while CodexManager CI references Node.js 22. Linux and Node 22 reproduction remain pending.
3. codex-web has no repository test suite at this revision; build/TypeScript success is weaker than protocol integration coverage.
4. npm reported 8 audit findings in the pinned codex-web dependency graph. They were not changed during this baseline-only task.
5. Native runtime/install-script behavior was not smoke-tested beyond successful build preparation.
6. CodexManager remains `UNLICENSED` / all-rights-reserved. Future clean-room work must continue to avoid copying its implementation.
7. No real-account E2E was performed in M0.1; that belongs to the explicitly authorized M0.3 continuity experiment.

## Acceptance decision

M0.1 acceptance is satisfied:

- exact upstream SHAs and Codex CLI/App Server binary version are recorded;
- declared build/typecheck/tests were run where available, with final passing evidence and environmental failures retained;
- no production behavior or upstream tracked source was changed.

M0.2 is only marked `ready`; it was not executed in this task.
