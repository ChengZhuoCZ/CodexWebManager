# M5.3 upgrade, backup, rollback, and restart-persistence evidence

## Task record

- Repository: `ChengZhuoCZ/CodexWebManager`
- Branch: `codex/m5-3-upgrade-backup-rollback`
- Input commit: `fb00514474acdd9b91112959c0e2540aabe03a20`
- Dependency: M5.2 (`done`)
- Implementation mode: `clean-room`
- Architecture mode: `LIMITED_MODE`
- Expected output: a bounded deployment manager, private configuration/state snapshots,
  one-command rollback, runtime circuit-state persistence, operator documentation, and native
  Linux evidence.

No real account is configured or switched in M5.3. Upgrade and restart recovery do not imply that
an in-flight calculation resumes in place, and this task makes no seamless cross-account
continuity claim.

## Initial inspection

The M5.1 installer already retains immutable versioned releases and atomically replaces the
`current` symlink. The M5.2 service owns a private state directory. However, no coordinated
configuration/state snapshot or rollback command exists, and the existing atomic circuit-state
store is not connected to runtime startup, failure mutations, shutdown, or the systemd unit.

Test-first commands and final evidence will be appended as the task proceeds.
