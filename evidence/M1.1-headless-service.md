# M1.1 headless service evidence

Task: M1.1 — Create headless service package

Input revision: `1eb233011f1e5a8a895c94002500db855fb379ab`

Output revision: `337df9f`

Branch: `codex/m1-1-headless-service`

Implementation mode: `clean-room`

Architecture mode: `LIMITED_MODE`

## Output

`packages/account-router/` contains a zero-dependency Node.js ESM service foundation with:

- literal loopback-only admin binding;
- default `127.0.0.1:18318` configuration;
- `GET`/`HEAD /healthz` process liveness;
- `GET`/`HEAD /readyz` readiness;
- explicit `503 no_accounts` when started without account providers;
- fail-closed readiness when the future account-count provider is invalid or throws;
- JSON-only, no-store health responses;
- bounded HTTP header/request/keep-alive timeouts;
- clean SIGINT/SIGTERM shutdown;
- no production dependency on Electron, X11, Wayland, a desktop keychain, or any third-party npm
  package.

## Commands and results

```text
/Users/chengzhuo/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node --version
# v24.14.0

/Users/chengzhuo/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node --check <all src/test .mjs files>
# passed; 7 files, 0 syntax failures

(cwd: packages/account-router)
/Users/chengzhuo/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node --test
# passed; 10 tests, 0 failures, duration 91.896083 ms
```

The integration test launches `src/main.mjs` with port `0` and without `DISPLAY`,
`WAYLAND_DISPLAY`, or `XAUTHORITY`, verifies the default bind address is `127.0.0.1`, calls
`/healthz`, sends SIGTERM, and verifies a clean exit.

## Acceptance mapping

- Node.js 22+: package engine is `>=22`; tests ran on Node.js 24.14.0.
- No Electron/graphics/keychain dependency: manifest has no dependencies and source-import tests
  permit only `node:` or local modules.
- Starts with no accounts: integration test starts the service without any account provider.
- Health/readiness: `/healthz` returns 200; `/readyz` returns explicit 503 `no_accounts`.
- Loopback by default: config and spawned CLI both resolve to literal `127.0.0.1`; wildcard, public,
  hostname, and malformed addresses are rejected.

## Deferred scope and risks

- Account metadata and secret providers remain M1.2.
- Authenticated admin/status/event APIs remain M1.3.
- Model proxy, SSE, scheduling, and failover remain M2/M3.
- Real account-switch tests remain deferred; this package performs no switch and makes no seamless
  continuity claim.
