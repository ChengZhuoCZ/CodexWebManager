# M6.9 routed codex-web failover and performance

## Result

M6.9 completed the clean-room failover implementation, fixture qualification,
single-account live path, and isolated 8216 deployment work through release
r16. Release r15 disabled authenticated Statsig event collection after the
public Statsig implementation was used to explain page-side event-drop
warnings. Release r16 moves the already-versioned main-module resource hint
ahead of the synchronous Tailnet startup shim so a cold multi-megabyte
transfer no longer waits for that shim's Tailnet round trip.

The r16 change modifies only deterministic `index.html` resource-discovery
order. It preserves script execution order, the main asset URL and bytes,
account selection, model requests, App Server IPC, network routes, and the
independent 8215 deployment. The live page retained a signed-in composer,
completed one fixed non-private single-account model sentinel, and produced
zero Statsig/manual-flush/event-drop warnings and zero page error logs.

End-to-end browser startup remained variable, so no stable startup-speed or
response-latency guarantee is claimed. The task also remains blocked at the
real two-account gate because the server has one configured, enabled, usable
credential. No real A-to-B switch was attempted, and no seamless
cross-account continuity or in-flight computation-resume claim is made.

Public mature-project behavior was used only as design input:
[`0xcaff/codex-web`](https://github.com/0xcaff/codex-web) informed the
long-lived App Server/thin-wrapper boundary,
[`codex app-server`](https://github.com/openai/codex/blob/main/codex-rs/app-server/README.md)
defined the connection lifecycle,
[`@fastify/static`](https://github.com/fastify/fastify-static) supplied the
existing precompressed-file path, and
[`statsig-io/js-client-monorepo`](https://github.com/statsig-io/js-client-monorepo)
was inspected to distinguish disabled event collection from a failed network
override. All integration code remains a small clean-room adapter; no
implementation was copied.

## Input and output

- Repository: `ChengZhuoCZ/CodexWebManager`
- Branch: `codex/m6-9-routed-failover-performance`
- Task input revision: `d41f2f282390f3f95e6795dd713d657b51788cc5`
- r15 implementation revision:
  `151066ba29d32042f1addbb69a5341951fc8ed65`
- r16 continuation input: `bd8be6a6a8b5b6dd5371fc089f94435f34eab3ff`
- r16 implementation revision:
  `aba6568899b70666907f7667c4e0e3c5ad62dba0`
- Router release: `codex-account-router-0.2.2-linux-x64`
- Routed codex-web release: `c3e92f0f-20260729-m69-router-r16`

## Automatic failover integration

The routed App Server remains fixed to the loopback account-router model
origin. The complete HTTP/SSE and WebSocket fixture matrix covers quota, 429,
authentication, network, upstream-5xx, malformed pre-semantic SSE, and
post-semantic refusal. Attempts, backoff, and the whole-pool deadline are
bounded. A request is never replayed after semantic SSE or WebSocket output.

The installed `codex-router-account enroll` workflow accepts only a private
regular Codex auth file path, validates it before changing state, uses a
private lock and atomic file operations, installs a root-owned mode `0600`
systemd credential, updates only the public account catalog, restarts only the
account router, requires all configured accounts to become usable, and rolls
back on failure. Its output contains only bounded counts and never a token,
email, provider account identifier, or full quota response.

The server still exposes one configured account in the browser status panel.
The enrollment command was not invoked because no second authorized private
credential was staged.

## Startup and response-path performance

### Reproducible assets

The source was reproduced from pinned upstream revision `c3e92f0fc16d...` by
applying the reviewed compatibility, startup, IPC Statsig, and authenticated
Statsig logging patches. Every patch passed a real dry-run before application.

The builder pins esbuild `0.27.0`. The 22,822,156-byte main input is
14,138,835 bytes after minification, with 4,345,031-byte gzip and
3,294,106-byte Brotli siblings. Identity, decoded gzip, and decoded Brotli
match SHA-256
`5d211964500013a9c68eb546775cb6cb071b080591881109239fec10bebbc7e0`.
The output-hash URL is
`./assets/app-initial-BTphDPeq.js?v=5d211964500013a9`.

The unchanged preload is 253,451 bytes identity, 79,865 bytes gzip, and
69,315 bytes Brotli. The unchanged initial stylesheet is 79,327 bytes
identity, 14,921 bytes gzip, and 13,143 bytes Brotli. The r16 index is 15,629
bytes with SHA-256
`154633d5fd2cdd26b06b699de69c16bc6f6d7804d1d97882e605c36ce4391bac`.
All inputs, outputs, and compressed decodes were hash-checked before
activation.

### r16 resource-discovery experiment

Before r16, the HTML parser encountered the synchronous
`tailnet-startup-fastpath.js` request before the main-module preload. From the
Mac over Tailnet:

- HTML TTFB was 329.478-331.802 ms and total time was 497.809-500.189 ms;
- the 1,942-byte synchronous shim TTFB was 330.307-332.061 ms and total time
  was 330.499-332.133 ms;
- main-module TTFB was 331.412-335.984 ms, while its three transfer totals
  varied at 2.216, 8.671, and 2.375 seconds.

This establishes a deterministic avoidable dependency: discovery of the cold
main transfer waited behind roughly one 330 ms Tailnet round trip. The r16
builder fails closed unless it can relocate exactly one versioned main-module
hint next to the import map and ahead of the synchronous shim. It does not
move or defer either script.

The live r16 DOM confirmed one import map and one main preload with head child
indices 2 and 3, followed by the startup shim and preload module at indices 4
and 5. The mapped and preloaded URL remained identical to r15, so existing
immutable caches were preserved instead of forcing a multi-megabyte
redownload.

Three cached r16 navigations reached the signed-in composer in bounded
observations of 7,118, 8,104, and 10,452 ms. These remain within the broad
historical variance and do not prove a stable end-to-end improvement. The
removed cold discovery dependency is deterministic; its actual wall-clock
benefit still depends on cache state, connection scheduling, transfer, parse,
and renderer work.

One fixed non-private model sentinel completed in 6,842 ms under 500 ms
polling. That is an observation, not a latency guarantee, and no prompt or
response body was retained in repository or server evidence.

### App Server boundary

The long-lived App Server Unix-socket probe remained:

- initialize: 15.8 ms;
- first round: account/read 1.7 ms, model/list 1.7 ms, thread/list 23.2 ms,
  config/read 2.4 ms;
- second round: account/read 1.5 ms, model/list 1.5 ms, thread/list 6.7 ms,
  config/read 1.9 ms.

The probe printed only method, status, timing, and count. It retained no
account, model, thread, prompt, or response body. These measurements rule out
sampled App Server startup RPCs as the remaining multi-second component.
Large-bundle transfer, parsing, and renderer boot remain likely contributors;
that attribution is an inference, not a proved causal breakdown.

### Static delivery

The live r16 main Brotli response was 3,294,106 bytes and the gzip response
was 4,345,031 bytes. Both returned HTTP 200 with the expected content
encoding, JavaScript content type, `Vary: Accept-Encoding`, and immutable
cache policy. The static-serving implementation was unchanged from r14, whose
complete Brotli/gzip/identity, q-value, range, ETag, and conditional-304
matrix remains applicable.

## Telemetry experiment

Page diagnostics on r14 showed genuine manual Statsig flush failures and 99
dropped events. Public Statsig source shows that
`preventAllNetworkTraffic` returns before `networkOverrideFunc`, and the event
sender represents the absent response as a failed status. The authenticated
Codex client options omitted the `loggingEnabled: "disabled"` setting already
present in its pre-login options.

The r15 patch disables collection at that authenticated client boundary. The
r16 page retained zero Statsig/manual-flush/event-drop warnings and zero error
logs after navigation and the model check. Its only page console entry was an
existing React route warning.

Separate network errors carrying a Browser control plugin client key were
traced to the local Browser control plugin bundle, not to the served codex-web
bundle. They are excluded from page-performance evidence and were not used to
justify a codex-web network change.

## Log boundary

The routed unit discards compatibility standard output and retains only
standard error in the journal. From r16 activation through the live model
check, 15 bounded journal lines were present with:

- zero syntax, chunk-load, unhandled, module, or request-error matches;
- zero authorization, cookie, bearer, email, prompt-sentinel, or other
  sensitive-shape matches;
- one systemd priority entry emitted by `nologin`.

The `nologin` entry has the same 48-byte message hash
`04d0ec641b0286ade0653c7be75090beaae7357ecc9f78c181e857b43c2e03a6`
as the host event recorded before r15. It is therefore a pre-existing host
event, not a new r16 application error. Historical records created before the
current log boundary remain a retention/access risk and must be handled
through the server's journal policy.

## Isolation of ports 8214 and 8215

Only `codex-web-router.service` was restarted for r16. The standalone upstream
8215 service remained active, returned HTTP 200, and retained its original
`ActiveEnterTimestamp` of `2026-07-29 04:02:49 CST`. The long-lived routed App
Server remained active and retained `2026-07-29 04:30:41 CST`. The
user-facing 8214 deployment was not changed. Routed 8216 entered active state
for r16 at `2026-07-29 09:03:17 CST` and returned HTTP 200.

Final runtime:

```text
codex-web-upstream.service               active (8215 HTTP 200, unchanged)
codex-account-router.service             active
codex-web-router-app-server.service      active (not restarted)
codex-web-router.service                 active (8216 HTTP 200)
routed stdout                            null
routed stderr                            journal
routed release                           c3e92f0f-20260729-m69-router-r16
```

## Verification

The strict matrix used its contract-pinned
`888692f7d885118c6a92bbaf60cf2121f5947adf` fixture. The live 8216 deployment
remains pinned to upstream revision `c3e92f0fc16d...`; neither contract was
weakened.

- test-first ordering assertion: failed against r15 behavior, then passed
  after the r16 implementation;
- focused r16 builder/patch tests: 8/8;
- complete direct Node discovery: 267 tests, 242 passed, 25
  fixture-dependent skips, 0 failures;
- strict release matrix: 256/256, no skips (unit 140, integration 69, E2E 47);
- focused security tests: 82/82 at revision `aba6568`;
- repository security scan: 318 files, 0 findings;
- strict historical UI baseline: 492/492;
- live r16 browser: ordered early resource hint, signed-in composer, and one
  minimal single-account model sentinel completed;
- live r16 page logs: zero Statsig/event-drop warnings and zero errors;
- live r16 HTTP: index hash, Brotli/gzip encodings, cache policy, `Vary`, and
  content type passed;
- r16 journal scan: 15 lines, zero application-error or sensitive-pattern
  matches; one matched pre-existing host `nologin` entry;
- final services active; 8215 and 8216 HTTP 200; 8215 and App Server
  activation timestamps unchanged.

Non-final attempts remain machine-readable. The restricted sandbox denied
loopback listeners, and the identical tests passed outside that restriction.
An experiment removing non-error console calls saved only 534 Brotli bytes and
was rejected as immaterial. No r16 deployment attempt changed or restarted
8215.

Detailed results are in `evidence/M6.9/test-summary.json` and
`evidence/M6.9/linux-deployment-summary.json`.

## Blocker and residual risk

The real A-to-B acceptance step requires a second user-authorized Codex
credential to be staged privately on the server. Fixtures prove the routing
policy but cannot prove provider behavior across two real accounts. The r16
cold resource-discovery dependency, r15 telemetry warning removal, and static
delivery boundary are qualified, while end-to-end startup and model latency
remain variable. See `evidence/M6.9/blocker.md`.
