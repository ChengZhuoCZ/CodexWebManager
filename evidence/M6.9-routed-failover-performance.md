# M6.9 routed codex-web failover and performance

## Result

M6.9 completed the implementation, fixture qualification, single-account live
path, and isolated 8216 deployment work. The task remains blocked at the real
two-account gate because the server has one configured, enabled, usable
credential. No real A-to-B switch was attempted, and no seamless
cross-account continuity or in-flight computation-resume claim is made.

Implementation mode remained `clean-room`. Public mature-project behavior was
used only as design input: thin upstream wrapping, explicit account
enrollment/status tooling, bounded whole-pool request budgets, cooldowns, and
fail-closed diagnostics. No implementation was copied.

## Input and output

- Repository: `ChengZhuoCZ/CodexWebManager`
- Branch: `codex/m6-9-routed-failover-performance`
- Input revision: `d41f2f282390f3f95e6795dd713d657b51788cc5`
- Implementation revision: `3a4c669642c06132bebae5ab9333fc2839f2541c`
- Router release: `codex-account-router-0.2.2-linux-x64`
- Routed codex-web release: `c3e92f0f-20260729-m69-router-r9`

## Automatic failover integration

The routed App Server remains fixed to the loopback account-router model
origin. Existing HTTP/SSE and WebSocket state machines passed the complete
quota, 429, authentication, network, upstream-5xx, malformed-pre-semantic-SSE,
and post-semantic refusal matrix.

The new `codex-router-account enroll` command makes a second private binding
operationally safe:

- accepts an absolute path to a private regular Codex auth file, never a token
  value on the command line;
- validates the credential structure before changing service state;
- rejects identifying aliases and duplicate public bindings;
- uses a private concurrency lock and atomic file operations;
- installs a root-owned mode `0600` systemd credential;
- atomically updates the public account catalog;
- restarts only `codex-account-router.service`;
- requires every configured account to become usable;
- restores both files and the previous router process if restart/readiness
  fails;
- emits only a bounded result with account count and
  `credentials_exposed=false`.

The server currently reports one configured, enabled, usable account and one
matching private account credential. The command is installed and its help
probe succeeds, but it was not invoked with a second real credential.

## Startup and response-path performance

The 22,822,120-byte content-hashed desktop bundle was already served as a
5,116,196-byte gzip file. A build-time Brotli artifact reduces the transferred
body to 3,739,265 bytes, 26.9% smaller than gzip. The server prefers `br`,
falls back to `gzip`, preserves identity responses, emits
`Vary: Accept-Encoding`, and retains immutable content-hash caching.
All three decoded bodies were byte-equal.

The connection-reuse experiment proved two sequential routed requests already
share one completed upstream HTTP connection, so no speculative proxy change
was made.

Browser timing still measured approximately 5.7 to 10.3 seconds on repeated
composer entries because the 22.8 MB decoded desktop bundle must be parsed and
initialized. The existing loader used a transparent background, which exposed
the browser's white canvas during that work. Release r9 gives the loader an
explicit light/dark background. A deployed dark-mode check reported
`electron-dark`, `rgb(24 24 24)`, a visible composer, and 6,531 ms to the usable
home screen. This removes the white flash; it does not claim to remove the
remaining desktop-bundle parse time.

One fixed non-private single-account browser sentinel completed after active
generation was observed. The post-fix run took 7,652 ms. Prompt and response
bodies were not stored in repository evidence.

## Log boundary correction

The live model check revealed that the upstream Electron compatibility stub
printed complete IPC messages and upstream response headers to standard
output. The routed unit now sets `StandardOutput=null` while retaining
`StandardError=journal`. A post-change model sentinel produced five bounded
journal lines, zero sentinel matches, zero Electron IPC matches, and zero
authorization, cookie, account, body, access-token, or refresh-token pattern
matches.

Historical journal records created before this boundary cannot be deleted by
unit without vacuuming unrelated system logs. They remain a retention/access
risk and must be handled by the server's journal retention policy.

## Isolation of port 8215

Only the account router and routed 8216 unit were restarted. The standalone
8215 service remained active and HTTP 200 before and after every activation.
Its final `ActiveEnterTimestamp` was `2026-07-29 04:02:49 CST`, earlier than
all M6.9 deployment changes.

Final runtime:

```text
codex-account-router.service             active
codex-web-router-app-server.service      active
codex-web-router.service                 active
codex-web-upstream-app-server.service    active
codex-web-upstream.service               active
8215                                     HTTP 200
8216                                     HTTP 200
router readiness                         ready, usable_accounts=1
routed stdout                            null
routed stderr                            journal
```

## Verification

The strict matrix used its contract-pinned `888692f7d885...` upstream fixture;
the independent live 8216 deployment remains pinned to
`c3e92f0fc16d...`. Mixing those pins was rejected during qualification rather
than weakening either contract.

- complete direct Node run: 253 tests, 228 passed, 25 environment-dependent
  skips, 0 failures;
- strict matrix with the clean pinned fixture: 253 passed, 0 failed, 0
  skipped;
- focused security tests: 82 passed, 0 failed, 0 skipped;
- repository security scan: 316 files, 0 findings;
- strict historical UI baseline: 492 passed, 0 failed, 0 skipped;
- live 8216 browser: signed-in composer present, one minimal single-account
  request completed;
- post-fix journal scan: no sentinel, IPC, credential, account, cookie, or
  body markers;
- final 8215 and 8216 probes: HTTP 200.

Detailed machine-readable results are in
`evidence/M6.9/test-summary.json` and
`evidence/M6.9/linux-deployment-summary.json`.

## Blocker and residual risk

The real A-to-B acceptance step requires a second user-authorized Codex
credential to be staged privately on the server. Until it exists, fixtures
prove routing policy but cannot prove provider behavior across two real
accounts. See `evidence/M6.9/blocker.md`.
