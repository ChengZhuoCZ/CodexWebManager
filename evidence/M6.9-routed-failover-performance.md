# M6.9 routed codex-web failover and performance

## Result

M6.9 completed the clean-room failover implementation, fixture qualification,
single-account live path, and isolated 8216 deployment work through release
r20. Release r15 disabled authenticated Statsig event collection after the
public Statsig implementation was used to explain page-side event-drop
warnings. Release r16 moves the already-versioned main-module resource hint
ahead of the synchronous Tailnet startup shim so a cold multi-megabyte
transfer no longer waits for that shim's Tailnet round trip. Release r17
hash-pins and inlines the 1,942-byte shim at the same execution position, so
the HTML parser no longer performs its separate revalidation request. Release
r18 adds deterministic Brotli and gzip siblings for the final HTML without
changing its identity bytes. Release r19 adds an optional, fail-closed,
version-pinned Terser second stage that reduces the main Brotli response by
120,417 bytes while preserving the established adapter boundaries. Release
r20 gives the preload bundle a deterministic eight-character content-hash
filename, allowing the existing upstream cache policy to serve that
representation immutably instead of revalidating it on every navigation.

The r20 change modifies only deterministic static assets. It preserves script
execution order, account selection, model requests, App Server IPC, and network
routes. The live page retained a signed-in composer and completed a fixed
non-private single-account model sentinel. A later explicit user-authorized
interrupt changed only the standalone 8215 account credential and services;
both routed 8216 activation timestamps remained unchanged.
Another explicit 8215-only interrupt installed a rollback-capable Full Access
override for the standalone App Server. It left all three 8216 PIDs and
activation timestamps unchanged.

End-to-end browser startup remained variable, so no stable startup-speed or
response-latency guarantee is claimed. The task also remains blocked at the
real two-account gate because the newly authorized standalone credential is
isolated to 8215 and has not been enrolled into the router. No real A-to-B
switch was attempted, and no seamless
cross-account continuity or in-flight computation-resume claim is made.

Public mature-project behavior was used only as design input:
[`0xcaff/codex-web`](https://github.com/0xcaff/codex-web) informed the
long-lived App Server/thin-wrapper boundary,
[`codex app-server`](https://github.com/openai/codex/blob/main/codex-rs/app-server/README.md)
defined the connection lifecycle,
[`@fastify/static`](https://github.com/fastify/fastify-static) supplied the
existing precompressed-file path, and
[`statsig-io/js-client-monorepo`](https://github.com/statsig-io/js-client-monorepo)
was inspected to distinguish disabled event collection from a failed network
override. All integration code remains a small clean-room adapter; no
implementation was copied.

## Input and output

- Repository: `ChengZhuoCZ/CodexWebManager`
- Branch: `codex/m6-9-routed-failover-performance`
- Task input revision: `d41f2f282390f3f95e6795dd713d657b51788cc5`
- r15 implementation revision:
  `151066ba29d32042f1addbb69a5341951fc8ed65`
- r16 continuation input: `bd8be6a6a8b5b6dd5371fc089f94435f34eab3ff`
- r16 implementation revision:
  `aba6568899b70666907f7667c4e0e3c5ad62dba0`
- r17 continuation input:
  `66da7a9094baa741faef623e5d6727350ab831ef`
- r17 implementation revision:
  `d001bd6ec29861272aeb7de087b7c9a904b84c41`
- r18 implementation revision:
  `750268a636a5f6d35e3a627ad3301ad3f7631d0e`
- r19 continuation input:
  `2c29e3a895feefd5c9a62f8bf8b699e0a7dd408f`
- r19 implementation revision:
  `40b2f1715a7691e85e3ed0303cccfd5ef6f6a2e8`
- r20 continuation input:
  `90a87e020cb9c4f6387656399603ec3893524f59`
- r20 implementation revision:
  `83d062d337c75c05bb4797d442fe1d8fe4ad255b`
- Standalone 8215 Full Access input revision:
  `312bd43ab6468773b6ae11a6357d82f5ebd27f7a`
- Standalone 8215 Full Access implementation revision:
  `acac3d5fdf6a77db8d84d85c3537b5d19614114f`
- Standalone 8215 Full Access evidence revision:
  `5c84482abe2c6b4d47ef5722a537d039b3b0b257`
- User-authorized 8215 logging-boundary revision:
  `d75c655c8fb3e3066245fec4060abd7aec5c4a46`
- Router release: `codex-account-router-0.2.2-linux-x64`
- Routed codex-web release: `c3e92f0f-20260729-m69-router-r20`
- Standalone codex-web release:
  `c3e92f0-20260729-tailnet-startup-r3`

## Automatic failover integration

The routed App Server remains fixed to the loopback account-router model
origin. The complete HTTP/SSE and WebSocket fixture matrix covers quota, 429,
authentication, network, upstream-5xx, malformed pre-semantic SSE, and
post-semantic refusal. Attempts, backoff, and the whole-pool deadline are
bounded. A request is never replayed after semantic SSE or WebSocket output.

The installed `codex-router-account enroll` workflow accepts only a private
regular Codex auth file path, validates it before changing state, uses a
private lock and atomic file operations, installs a root-owned mode `0600`
systemd credential, updates only the public account catalog, restarts only the
account router, requires all configured accounts to become usable, and rolls
back on failure. Its output contains only bounded counts and never a token,
email, provider account identifier, or full quota response.

The router still exposes one configured account in the browser status panel.
The separate credential authorized for standalone 8215 was deliberately not
copied into router storage: the user authorized an 8215 account change, not
router enrollment. This preserves the credential-scope boundary.

## Startup and response-path performance

### Reproducible assets

The source was reproduced from pinned upstream revision `c3e92f0fc16d...` by
applying the reviewed compatibility, startup, IPC Statsig, and authenticated
Statsig logging patches. Every patch passed a real dry-run before application.

The builder pins esbuild `0.27.0`. The 22,822,156-byte main input is
14,138,835 bytes after minification, with 4,345,031-byte gzip and
3,294,106-byte Brotli siblings. Identity, decoded gzip, and decoded Brotli
match SHA-256
`5d211964500013a9c68eb546775cb6cb071b080591881109239fec10bebbc7e0`.
The output-hash URL is
`./assets/app-initial-BTphDPeq.js?v=5d211964500013a9`.

The unchanged preload is 253,451 bytes identity, 79,865 bytes gzip, and
69,315 bytes Brotli. The unchanged initial stylesheet is 79,327 bytes
identity, 14,921 bytes gzip, and 13,143 bytes Brotli. The r16 index is 15,629
bytes with SHA-256
`154633d5fd2cdd26b06b699de69c16bc6f6d7804d1d97882e605c36ce4391bac`.
The r17 index-only adapter accepts that exact r16 hash and the 1,942-byte
startup script hash
`3eb857920a3be9ceb4ff9be0c0746199dab0f8c4e4a02d3762380508a429aabd`.
It validates both regular-file boundaries, the same-directory layout, script
syntax, absence of an HTML closing-script boundary, versioned import-map/hint
URL equality, and import-map/hint/shim/preload order before an atomic rename.
The resulting r17 index is 17,576 bytes with SHA-256
`1b7845677d03bbc104e14a55483cdb3fcb94ed6d4ea83e60e1655e19d10fa9e1`.
The main, preload, stylesheet, compressed siblings, and immutable main URL
remain byte-identical to r16. All inputs, outputs, and compressed decodes were
hash-checked before activation.

### r16 resource-discovery experiment

Before r16, the HTML parser encountered the synchronous
`tailnet-startup-fastpath.js` request before the main-module preload. From the
Mac over Tailnet:

- HTML TTFB was 329.478-331.802 ms and total time was 497.809-500.189 ms;
- the 1,942-byte synchronous shim TTFB was 330.307-332.061 ms and total time
  was 330.499-332.133 ms;
- main-module TTFB was 331.412-335.984 ms, while its three transfer totals
  varied at 2.216, 8.671, and 2.375 seconds.

This establishes a deterministic avoidable dependency: discovery of the cold
main transfer waited behind roughly one 330 ms Tailnet round trip. The r16
builder fails closed unless it can relocate exactly one versioned main-module
hint next to the import map and ahead of the synchronous shim. It does not
move or defer either script.

The live r16 DOM confirmed one import map and one main preload with head child
indices 2 and 3, followed by the startup shim and preload module at indices 4
and 5. The mapped and preloaded URL remained identical to r15, so existing
immutable caches were preserved instead of forcing a multi-megabyte
redownload.

Three cached r16 navigations reached the signed-in composer in bounded
observations of 7,118, 8,104, and 10,452 ms. These remain within the broad
historical variance and do not prove a stable end-to-end improvement. The
removed cold discovery dependency is deterministic; its actual wall-clock
benefit still depends on cache state, connection scheduling, transfer, parse,
and renderer work.

One fixed non-private model sentinel completed in 6,842 ms under 500 ms
polling. That is an observation, not a latency guarantee, and no prompt or
response body was retained in repository or server evidence.

### r17 synchronous-shim request experiment

The r16 HTML still contained a synchronous external
`tailnet-startup-fastpath.js` element. Its route used
`Cache-Control: public, max-age=0`, so even a cached browser revalidated the
1,942-byte file across the Tailnet. The earlier direct probes measured that
request at 330.499-332.133 ms total. Adding its fixed source to the 15.6 KB
HTML costs 1,947 index bytes but removes that request from the parser's
blocking dependency.

Test-first evidence changed the expected external script to one inline marker;
the focused test failed against r16 behavior and passed after implementation.
The adapter also rejects a wrong index or script hash, unsafe closing-script
source, wrong resource order, and a second application to an already-inlined
index while leaving the last valid file intact.

The live r17 DOM contained one inline marker and zero external startup-shim
script elements. It retained one import map, one matching versioned main hint,
the unchanged preload module, the signed-in composer, and no alert surface.
Two same-tab cached navigations reached the composer in 14,330 and 18,845 ms;
a separate first-tab observation was only an upper bound of 24,593 ms because
browser-control scheduling occurred between navigation and the readiness
poll. These observations are slower and more variable than the r16 samples,
so they do not demonstrate a stable end-to-end speedup. The eliminated
external request is deterministic; main transfer, parse, and renderer work
still dominate the variable remainder.

Two fixed non-private single-account model sentinels completed. One timing run
completed in 8,917 ms under 250 ms polling. This is an observation, not a
guarantee. No prompt or response body was written to repository evidence or
server logs, and no account switch was requested.

### r18 HTML precompression

Before r18, `Accept-Encoding: br` and `gzip` on the final 17,576-byte HTML
still returned identity bytes. A test-first assertion for `index.html.br` and
`index.html.gz` failed with missing files, then passed after the builder and
index-only successor both generated hash-checked siblings.

The r18 release keeps identity SHA-256
`1b7845677d03bbc104e14a55483cdb3fcb94ed6d4ea83e60e1655e19d10fa9e1`.
Its Brotli representation is 4,149 bytes with SHA-256
`5aa50067197a9ceebe8cb4efcabd1f7d7baaba9bb195d6ff7b22df54b54a0cd6`;
its gzip representation is 5,960 bytes with SHA-256
`5cc228cc53565ff7c08e49ae6502a363cf658f151800ea49da763d94777cb0e6`.
Both decode to the exact identity document.

Three external Tailnet probes observed:

- identity total time 506.715-537.028 ms for 17,576 bytes;
- Brotli total time 341.677-434.442 ms for 4,149 bytes;
- gzip total time 340.402-349.325 ms for 5,960 bytes.

The signed-in r18 composer rendered in a bounded 25,574 ms observation and one
fixed non-private model sentinel completed in 9,173 ms. These are live
qualification observations, not stable startup or response-latency claims.

### r19 pinned post-minification

The remaining main transfer dominated the static payload. Re-encoding the
r18 Brotli input with the available encoder settings produced the identical
3,294,106-byte body, so encoder tuning was rejected. Enabling esbuild
tree-shaking reduced identity by 5,588 bytes but increased Brotli by 100 bytes
and was also rejected. A conservative Terser pass retaining function and class
names produced mixed reductions below the one-percent acceptance threshold.

The accepted builder option pins Terser `5.49.0` and its official CLI entry
SHA-256
`312a3f9b37d3f5316ee384bfdc347313dae6f0f9056c44b3cae56c8e4e9f4496`.
It uses module parsing, ECMAScript 2022, two compression passes, identifier
mangling, and no property mangling. The builder fails closed unless identity
and gzip shrink and Brotli shrinks by at least one percent.

Two isolated runs produced byte-identical results:

- identity: 13,936,387 bytes, SHA-256
  `e2d356e06763a8287003e5a087acb09a09160a1d6bc8fbf9cecccdfdaf82b6b0`;
- Brotli: 3,173,689 bytes, SHA-256
  `0899d532efde663c13196219d5e89b355469969397739c2db890593740bf9be1`;
- gzip: 4,243,211 bytes, SHA-256
  `1c6c15a253441c4afd366e46dcadc3056a16d5be85c43ec755324c2064cc9a1c`.

The corrected output-hash URL is
`./assets/app-initial-BTphDPeq.js?v=e2d356e06763a828`. The final index uses
that URL exactly twice and is 17,576 bytes with SHA-256
`ccbf0a8318d6559939605f4b291f0bf50cd14539ea7fc2fe76b0923cf7152d88`.
Its 4,150-byte Brotli and 5,961-byte gzip representations have SHA-256
`78cb8fb70825554a0e911ed69dcf3b9e0487ef66482228f7409226a907416b64`
and
`5891e0c3f225392579a81e6930acd6af6515088ad4e95b228a1958976ff976cf`.

The fresh browser observation reached the signed-in composer in 28,315 ms and
completed one fixed model sentinel in 3,673 ms. Three later observations were
22,745 ms, a 29,439 ms observation cutoff without composer readiness, and
16,537 ms. The payload reduction is deterministic; these variable browser
samples do not establish a stable startup-speed improvement.

### r20 content-hashed preload cache identity

The remaining 253,451-byte preload module still used the unversioned
`./assets/preload.js` URL and therefore received `Cache-Control: public,
max-age=0`. The r20 builder now derives
`./assets/preload-65708a1c.js` from the first eight hexadecimal characters of
the qualified preload identity SHA-256. Eight characters are intentional: the
running upstream server recognizes exactly that filename shape as immutable.
The original unversioned identity/gzip/Brotli triplet is retained for
compatibility, while the final HTML references only the versioned module.

Two clean builds from the pinned upstream inputs were byte-identical. The
versioned and retained unversioned preload files also match byte-for-byte:

- identity: 253,451 bytes, SHA-256
  `65708a1c2c053568691f6691b76290a6bd07df09ea84ef1186ac77090649fc5a`;
- gzip: 79,865 bytes, SHA-256
  `ee83a0334b52d8557a7b4252e02addf34687082b584a769f06091c2dc5355d32`;
- Brotli: 69,315 bytes, SHA-256
  `815f74c342f4046a6c1d0e01276adfb5dca47a0ec0771532f5bedfae62b07b68`.

The main module and its output-hash URL remained unchanged from r19. The final
r20 HTML is 17,585 bytes with SHA-256
`20703fc1b770eeb40a23fd604b46ed5efb5f2d95a93eb7e51cec30b39ef60d59`;
its 5,967-byte gzip and 4,164-byte Brotli siblings have SHA-256
`3f497714830ce20ec895cb9211fd3b690f5695a1e71ef8fbaa06d83c88a9cf21`
and
`618f6e08f54baecba5eebdceeed74b4d75030f846d8848e0e02730489b573c11`.

The no-credential deployment script pins the 248,567-byte archive SHA-256,
allowlists all 11 archive members, verifies all identity/compressed hashes and
decompression, and checks the standalone 8215 PID plus start timestamp before
and after activation. The final live versioned preload returned HTTP 200 with
`public, max-age=31536000, immutable`, `Vary: Accept-Encoding`, Brotli
encoding, and the expected 69,315-byte body. The retained
`./assets/preload.js` path remained HTTP 200 with `max-age=0`.

The browser reached the signed-in composer and returned the exact fixed
non-private model sentinel. A warm reload retained the composer, but the
measurement was polluted by browser-tool scheduling and is not used as a
latency sample. No stable startup or response-speed improvement is claimed.

Two activation attempts were rejected before the final success. The first
probed the model listener instead of the router admin readiness listener and
rolled all 8216 services back. The second used a 16-character filename; its
bytes passed but the upstream exact eight-character cache rule returned
`max-age=0`, so the script again rolled back. The corrected eight-character
candidate then passed the complete deployment verifier. Neither rollback
changed or restarted 8215.

### App Server boundary

The long-lived App Server Unix-socket probe remained:

- initialize: 15.8 ms;
- first round: account/read 1.7 ms, model/list 1.7 ms, thread/list 23.2 ms,
  config/read 2.4 ms;
- second round: account/read 1.5 ms, model/list 1.5 ms, thread/list 6.7 ms,
  config/read 1.9 ms.

The probe printed only method, status, timing, and count. It retained no
account, model, thread, prompt, or response body. These measurements rule out
sampled App Server startup RPCs as the remaining multi-second component.
Large-bundle transfer, parsing, and renderer boot remain likely contributors;
that attribution is an inference, not a proved causal breakdown.

### Static delivery

The live r19 main Brotli response is 3,173,689 bytes and returned HTTP 200
with the expected content encoding, JavaScript content type,
`Vary: Accept-Encoding`, and immutable cache policy. The 4,243,211-byte gzip
sibling uses the same qualified static-serving code. That implementation was
unchanged from r14, whose complete
Brotli/gzip/identity, q-value, range, ETag, and conditional-304 matrix remains
applicable.

## Telemetry experiment

Page diagnostics on r14 showed genuine manual Statsig flush failures and 99
dropped events. Public Statsig source shows that
`preventAllNetworkTraffic` returns before `networkOverrideFunc`, and the event
sender represents the absent response as a failed status. The authenticated
Codex client options omitted the `loggingEnabled: "disabled"` setting already
present in its pre-login options.

The r15 patch disables collection at that authenticated client boundary. The
r17 page retained zero Statsig/manual-flush/event-drop warnings and zero error
logs after navigation and both model checks. Its only page console entry was
an existing React route warning.

Separate network errors carrying a Browser control plugin client key were
traced to the local Browser control plugin bundle, not to the served codex-web
bundle. They are excluded from page-performance evidence and were not used to
justify a codex-web network change.

## Log boundary

The routed unit discards compatibility standard output and retains only
standard error in the journal. From r17 activation through both live model
checks, 10 bounded journal lines were present with:

- zero authorization, cookie, bearer, email, prompt-sentinel, or other
  sensitive-shape matches;
- two priority-6 Node messages containing broad `error`/`failed` words whose
  lengths and SHA-256 values exactly matched r16 startup messages;
- one systemd priority-2 entry emitted by `nologin`.

The `nologin` entry has the same 48-byte message hash
`04d0ec641b0286ade0653c7be75090beaae7357ecc9f78c181e857b43c2e03a6`
as the host event recorded before r15. The two Node message hashes are
`ea05c1f2ce84409f595c968ea2bac5af575d6700c177f3c37c2c0190a16ba84e`
and
`6e575ab4c35cfd884cef306690e3773d4c42d4187a53aeeddbfc92561f39cb4a`,
identical in length, priority, and hash to r16 startup. They are therefore
pre-existing startup/host events, not new r17 application failures.
Historical records created before the current log boundary remain a
retention/access risk and must be handled through the server's journal policy.

## User-authorized standalone 8215 repair

The original M6.9 scope protected 8215 from changes. During r18 preparation the
user explicitly interrupted the task and authorized repair of 8215. Read-only
diagnosis reproduced a startup screen that eventually left an empty root while
HTTP and static resources remained 200. The old standalone release delivered a
22,822,156-byte uncompressed main module and had no Tailnet startup telemetry
or precompressed-file adapter.

The repair created immutable r2 and r3 successors and retained every previous
release for rollback. r2 added only the exact reviewed non-provider startup
and Statsig adapters. r3 used integrity-verified npm package
`@esbuild/linux-x64@0.27.0` to produce the same qualified 14,138,835-byte
identity main module, 3,294,106-byte Brotli body, and 4,345,031-byte gzip body
used by the routed build. No router UI, account configuration, credential, or
model endpoint was copied into the standalone release.

From the Mac over Tailnet, the standalone main-module transfer changed from a
45.491-second identity observation to 4.206 seconds with Brotli and 7.297
seconds with gzip. The signed-in composer rendered, and a post-boundary fixed
model sentinel completed in 3,728 ms. Browser startup still varied, so only the
payload and transfer observations are claimed.

The first r3 activation attempt correctly rolled back because its verifier did
not normalize CRLF response headers. The candidate body and Brotli decode were
valid. A corrected hash-pinned activation succeeded. The same test-first
logging rule already used by 8216 was then applied to the standalone systemd
unit: `StandardOutput=null`, `StandardError=journal`. After restart and a real
model sentinel, the bounded 8215 journal scan contained 10 rows, zero sensitive
shape matches, zero sentinel matches, and no new application error.

The user later explicitly authorized switching 8215 to another account. Device
authorization ran in a private isolated home and produced a mode-`0600` file;
its contents and account identity were never printed or retained as evidence.
A rollback-capable script atomically installed it as the root-owned
`standalone-upstream-auth.json` credential and added a standalone-only systemd
drop-in that resets the former shared `LoadCredential` list before selecting
that file. The first activation safely rolled back because an unprivileged
Unix-socket metadata check could not traverse the service's mode-`0700`
runtime directory. The corrected script used a scoped privileged socket check
and succeeded. It deleted the temporary login home after installation.

Only the standalone App Server and 8215 Web service restarted, at
`2026-07-29 13:16:01 CST` and `13:16:02 CST`. The browser opened directly to
the signed-in composer and one fixed model sentinel completed in 27,319 ms.
The 36-row bounded post-switch journal scan contained zero sensitive-shape
matches and zero sentinel matches. Both 8216 services stayed active with
unchanged activation timestamps; this standalone account change is not a
router A-to-B failover test.

## User-authorized standalone 8215 Full Access

The user later explicitly requested Codex Full Access on 8215 while preserving
the running 8216 deployment. The standalone-only systemd drop-in fixes Codex
to `sandbox_mode="danger-full-access"` and `approval_policy="never"` with CLI
overrides, which have the highest documented configuration precedence. It also
removes the outer systemd filesystem and address-family restrictions that had
kept the service read-only outside `/srv/codex-workspaces` and had prevented
the inner Linux `bwrap` sandbox from creating its network namespace.

The service still runs as the unprivileged `codex` Unix user. It was not
granted root or passwordless sudo, and ordinary Unix ownership and mode bits
still apply. This is therefore Codex Full Access for files and network
available to that user, not host-administrator authority.

A test-first assertion failed because the standalone drop-in did not yet
exist. After implementation, the focused systemd suite passed 14/14 and the
no-credential deployment script passed `bash -n`. The script snapshots the
router, routed App Server, and routed Web PIDs and monotonic start timestamps,
restarts only the standalone App Server, verifies its live process arguments
and effective systemd properties, and rolls the original configuration back
on any failure.

Two pre-final attempts reached an active 8215 service but rejected overly
strict evidence assertions; both automatically restored the original limited
configuration. The final activation completed at
`2026-07-29 19:09:21 CST`. The running App Server argv contains the two exact
Full Access overrides, all checked outer restrictions are disabled, 8215 and
8216 both return HTTP 200, and the bounded post-activation journal check found
zero `bwrap`, `NETLINK_ROUTE`, UID-map, or permission-denied sandbox errors.
A separate no-model `config/read` probe using the deployed CLI arguments
returned `sandbox_mode=danger-full-access` and `approval_policy=never` without
an RPC error.

Standalone Web PID `466019` and its monotonic start timestamp
`173291171117` did not change. Routed Web PID `495185`, routed App Server PID
`495033`, and router PID `495006` retained monotonic start timestamps
`188703695080`, `188703455553`, and `188703181053` respectively. No router
credential was changed, no model request was required, and no account switch
was executed.

## Runtime isolation of ports 8214, 8215, and 8216

Only `codex-web-upstream.service` was restarted during the explicit 8215
repair. Its App Server retained `2026-07-29 03:44:26 CST`; 8216 was not
restarted by that repair. Only `codex-web-router.service` was restarted for
r18. During r19 activation, repaired 8215 retained
`2026-07-29 11:57:33 CST`, and the routed App Server retained
`2026-07-29 04:30:41 CST`. During the later user-authorized 8215 account
change, the routed Web service retained `2026-07-29 12:51:36 CST` and its App
Server again retained `2026-07-29 04:30:41 CST`. The user-facing 8214
deployment was not changed. The final r20 activation started only the three
isolated 8216 units. Standalone 8215 retained Web PID `466019`, App Server PID
`465713`, and their `2026-07-29 13:16:02 CST` /
`2026-07-29 13:16:01 CST` start timestamps.

Final runtime:

```text
codex-web-upstream.service               active (8215 HTTP 200)
codex-account-router.service             active
codex-web-router-app-server.service      active
codex-web-router.service                 active (8216 HTTP 200)
standalone stdout                        null
standalone stderr                        journal
routed stdout                            null
routed stderr                            journal
standalone release                       c3e92f0-20260729-tailnet-startup-r3
routed release                           c3e92f0f-20260729-m69-router-r20
standalone credential source             isolated root-owned mode 0600 file
standalone Codex sandbox                 danger-full-access
standalone approval policy               never
standalone Unix identity                 codex:codex (not root)
```

## Verification

The strict matrix used its contract-pinned
`888692f7d885118c6a92bbaf60cf2121f5947adf` fixture. The live 8216 deployment
remains pinned to upstream revision `c3e92f0fc16d...`; neither contract was
weakened.

- test-first inline assertion: failed against r16 behavior, then passed after
  the r17 implementation;
- r20 test-first content-hash assertion: failed before implementation, then
  the focused builder/patch/index adapter suite passed 10/10;
- complete direct Node discovery: 270 tests, 245 passed, 25
  fixture-dependent skips, 0 failures;
- strict release matrix: 259/259, no skips (unit 143, integration 69, E2E 47);
- focused security tests: 83/83 at revision `83d062d`;
- repository security scan: 320 files, 0 findings;
- strict historical UI baseline: 492/492;
- live r18 browser: signed-in composer and one minimal single-account model
  sentinel completed;
- live r19 browser: corrected output-hash URL, signed-in composer, and one
  minimal single-account model sentinel completed;
- live r20 browser: content-hashed preload URL, signed-in composer, and one
  exact fixed single-account model sentinel completed;
- live standalone 8215 browser: signed-in composer and two minimal
  single-account model sentinels completed before the account change, then
  the newly authorized account reached the composer and completed one
  additional sentinel;
- live r17 page logs: zero Statsig/event-drop warnings and zero errors;
- live r18 HTTP: identity, Brotli, and gzip index hashes, encodings, `Vary`,
  and content type passed;
- r18 journal scan: 11 lines, zero sensitive-pattern or fixture-sentinel
  matches and no new application error;
- r19 journal scan: 10 lines, zero sensitive-pattern or fixture-sentinel
  matches;
- r20 journal scan: 19 lines, zero sensitive-pattern or fixture-sentinel
  matches; a title-generation 405 and model-manager refresh timeouts remain
  non-secret operational risks, while the fixed chat model path completed;
- live r20 preload HTTP: identity/gzip/Brotli hashes and decompression passed;
  the versioned URL is immutable and the retained unversioned URL remains
  revalidated;
- post-boundary 8215 journal scan: 10 lines, zero sensitive-pattern or
  fixture-sentinel matches and no new application error;
- post-account-change 8215 journal scan: 36 lines, zero sensitive-pattern or
  fixture-sentinel matches;
- standalone 8215 Full Access: test-first failure, then 14/14 focused systemd
  tests; live process argv and effective systemd properties passed; two
  assertion-only deployment failures rolled back; final HTTP 200 and zero
  post-activation Linux-sandbox error matches;
- final services active; 8215 and 8216 HTTP 200; each deployment script proved
  the out-of-scope service and its App Server timestamp unchanged.

Non-final attempts remain machine-readable. The restricted sandbox denied
loopback listeners, and the identical tests passed outside that restriction.
An experiment removing non-error console calls saved only 534 Brotli bytes and
was rejected as immaterial. r19 also rejected identical Brotli encoder
variants, tree-shaking that increased Brotli by 100 bytes, and a conservative
Terser result below the one-percent acceptance threshold. The first r17
archive was rejected before
activation because it carried a macOS provenance extended header; the final
6,074-byte ustar archive contained exactly one regular `index.html`. No r17
deployment attempt changed or restarted 8215. r20 additionally rejected a
locally unsupported BSD-tar option, a wrong readiness port, and a
16-character cache-key candidate; the two server-side attempts rolled back
before the final eight-character activation and preserved 8215. Two failed
explicit ProxyCommand uploads also left server state unchanged. A timing
sample initially labeled seconds as milliseconds was rejected rather than
used as performance evidence. The later standalone Full Access deployment
also rejected two over-strict post-activation assertions and restored the
limited configuration before the final corrected verifier succeeded.

Detailed results are in `evidence/M6.9/test-summary.json` and
`evidence/M6.9/linux-deployment-summary.json`.

## R21: Codex HTTP/SSE transport fallback

R20 exposed one non-secret auxiliary failure: Codex attempted
`POST /backend-api/codex/responses` and the router returned HTTP 405 because
only the WebSocket transport had been qualified. Current upstream Codex keeps
a transport-fallback state and can fall back from WebSocket to HTTP/SSE; see
the upstream
[`client.rs`](https://github.com/openai/codex/blob/main/codex-rs/core/src/client.rs)
and the corresponding upstream
[`#19821`](https://github.com/openai/codex/issues/19821) failure report.

R21 adds one exact clean-room route:

```text
POST /backend-api/codex/responses
transport=http
upstream=/backend-api/codex/responses
queries=none
```

The route is not a generic backend prefix. Absolute targets, suffixes,
queries, method mismatches, and all other backend paths remain rejected. It
uses the same bounded HTTP/SSE failover handler as `/v1/responses`:

- an explicitly classified account failure before semantic output may select
  another account within the existing attempt/backoff/deadline bounds;
- preflight SSE from the failed attempt is discarded;
- semantic output commits the request and any later failure returns
  `unsafe_to_replay` without selecting another account;
- a body carrying `previous_response_id` is a continuation and is never
  replayed to a replacement connection or account.

Test-first route and failover assertions failed with HTTP 405 before the
implementation. After implementation, the complete strict matrix passed
262/262: unit 145, integration 70, E2E 47. Security qualification passed
85/85 focused tests and scanned 326 repository files with zero findings.
The E2E cases use fixed no-credential fixtures and do not constitute a real
account switch.

The deterministic Linux x64 release is
`codex-account-router-0.2.4-linux-x64`, SHA-256
`8eef578534a56cf5778b0f0665337bfc1da5040d3118627327a8d4924428f7dd`.
Two builds were byte-identical. A Linux short test verified checksum,
installation over an existing `current` symlink, loopback-only startup,
health, no-account non-readiness, and clean SIGTERM without credentials.

The first live deployment attempt discovered that the 0.2.3 installer used
`mv -f` for an existing symlink to a directory. Its invariant failed before
the candidate router was activated, restored 0.2.2, restarted it ready, and
left both Web/App Server pairs unchanged. A regression now seeds exactly that
upgrade topology and the installer uses Linux `mv -T`. The corrected 0.2.4
deployment succeeded.

Only `codex-account-router.service` restarted. The final deployment preserved:

- standalone 8215 Web PID/start `510876`/`196497166049`;
- standalone 8215 App Server PID/start `508396`/`194490256322`;
- routed 8216 Web PID/start `495185`/`188703695080`;
- routed 8216 App Server PID/start `495033`/`188703455553`;
- both Web releases and all four effective unit-config digests.

The installed router normalized the exact HTTP fallback route and rejected a
query-bearing variant. Router readiness and both Web origins returned HTTP
200. A later stability probe observed one transient 503 while the sole
configured account was unavailable; the next 20 half-second samples were all
ready with one usable account. Given the unchanged process and the readiness
implementation, this is consistent with the sole account briefly reaching
its concurrency limit, but the discarded 503 body prevents a definitive
reason classification. Mac-to-Tailnet totals
were 0.485 seconds for 8215 and 0.500 seconds for 8216; these are reachability
samples, not a stable latency claim. No deliberate prompt, title-generation,
or model request was sent after R21, so the historical title-generation 405
is addressed at the router route/fixture/deployed-module layers but not
claimed as a completed live title-generation request. Background App Server
refresh traffic was not instrumented, and prior model-manager refresh
timeouts were not deliberately re-exercised.

During R21 the user separately asked a subagent to repair 8215. That repair
restarted only the standalone Web process after proving its one-shot Unix
socket proxy had not reconnected to a newer App Server socket. The App Server
and all 8216 processes remained unchanged. A standalone-only `PartOf=` drop-in
now couples an explicit future App Server restart to the Web service. R21
deployment captured a fresh post-repair 8215 baseline and preserved it.
Details are in
`evidence/M6.9/standalone-8215-startup-repair.md` and
`evidence/M6.9/router-r21-release-summary.json`.

## R22: account-scoped model-catalog cache

The remaining startup logs repeatedly showed model-manager refresh waits.
Before implementation, two minimal authorized reads of the exact observed
model-catalog route returned the same 302,492-byte JSON document and SHA-256:
the first took 2.487 seconds and the immediate second took 0.253 seconds. No
prompt, generation, or account switch was sent, and model names were not
recorded.

The clean-room design follows the bounded freshness principle used by the
upstream Codex models manager
[`cache.rs`](https://github.com/openai/codex/blob/main/codex-rs/models-manager/src/cache.rs),
while adding a router-specific isolation rule: an entry key contains both the
selected router account and the already-normalized upstream target, including
`client_version`. Therefore a catalog observed under account A is never served
as account B's catalog.

The in-memory cache is bounded to 32 entries, a five-minute TTL, and two MiB per
entry. It stores only successful, valid JSON for the exact `codex_models`
route. It never stores errors, credentials, cookies, upstream request IDs, or
rate-limit headers. Cached bodies are copied on write and read, expired entries
are never served, and least-recently-used eviction is deterministic. Search and
model-generation routes are not cached. Cache lookup occurs only after normal
scheduling has selected an account, so circuit, cooldown, quota, and account
availability still determine which account is eligible.

The final strict matrix passed 267/267 with no skips: unit 149, integration 71,
and E2E 47. Security qualification passed 85/85 focused tests and scanned 331
repository files with zero findings. These suites used fixed no-credential
fixtures and did not execute account switching.

The deterministic Linux x64 release is
`codex-account-router-0.2.5-linux-x64`, SHA-256
`4b2df22e926fb02324fcc547efe66f5eb6e1160e7492d7ed1f52439bc2d8465e`.
Two builds were byte-identical. On the target Linux host, the no-credential
verifier confirmed checksum, payload manifest, installation, loopback-only
listeners, HTTP 200 health, explicit `no_accounts` non-readiness, and clean
SIGTERM. The first verifier invocation was rejected before artifact execution
because the uploaded scripts lacked their expected directory layout; the
corrected layout then passed.

Only `codex-account-router.service` restarted, from PID 516181 on 0.2.4 to PID
524721 on 0.2.5. The deployment preserved all standalone 8215 and routed 8216
Web/App Server PIDs, start-monotonic timestamps, releases, and effective-unit
digests. The live model-catalog probe returned the same 302,492-byte content:
the cold request took 1.740 seconds and the fresh cache hit took 0.0023 seconds.
The first response carried an upstream request-ID header; the cached response
did not replay it. Twenty 0.2-second readiness samples were all HTTP 200, the
router journal had zero sensitive-marker matches, and both Tailnet Web origins
returned HTTP 200. These are bounded route measurements, not a stable
end-to-end browser speed claim.

During R22 the user again assigned the 8215 startup page to a subagent. Its
second independent review found every server-side layer healthy and performed
no restart or mutation. A fresh cache-busting page, WebSocket upgrade, Web IPC,
Unix-socket connection, account read, and seven-model catalog all succeeded
without a model request. The remaining old-tab symptom requires a hard refresh
or browser Console evidence. See
`evidence/M6.9/standalone-8215-startup-r2-review.md` and
`evidence/M6.9/router-r22-model-cache-summary.json`.

## R24: production runtime switch wiring

The R24 audit found that the protected switch endpoint and codex-web account
panel already existed, but the standalone runtime had not supplied the switch
callback. It also did not publish the selected production route or connect
semantic HTTP/WebSocket lifecycle events to `active_streams`. Consequently the
production switch endpoint returned `switch_not_available`, even though the
fixture-level admin and UI contracts passed.

The clean-room implementation now treats a validated manual selection as an
eligible in-memory preference for the next new request. It is checked by the
normal scheduler, cannot select a disabled, cooling-down, quota-exhausted or
concurrency-saturated account, is cleared when its credential is unavailable,
and does not move an existing response chain. The preference and current route
are intentionally not persisted across a router restart.

The runtime starts its active-stream guard on the first non-preflight semantic
HTTP SSE or WebSocket response event and releases it on normal completion,
upstream failure and unsafe-to-replay paths. Manual switching is checked both
before and after its asynchronous eligibility read. Automatic route changes
after a pre-semantic account failure publish only sanitized aliases, a bounded
failure reason, and `new_backend_session` continuity. Once semantic output has
started, the existing no-replay boundary remains authoritative.

The clean test-first runtime experiment failed three expected assertions:
production `current_route` remained null, `active_streams` remained zero during
a semantic SSE response, and a pre-semantic quota failover did not publish its
new route. The final focused suite passed 37/37. The strict matrix passed
272/272 with no skips: unit 150, integration 75 and E2E 47. Security
qualification passed 89/89 focused tests and 492/492 strict UI tests while
scanning 341 repository files with zero findings. All tests used no-credential
fixtures; no real account switch was executed.

The deterministic Linux x64 release is
`codex-account-router-0.2.6-linux-x64`, SHA-256
`718bbf6f24d9c2c742c064754af622501f05a3230c6a2670e6f7c60fce932c04`.
Two builds were byte-identical. The target-host no-credential verifier
confirmed its 48-file manifest, loopback-only listeners, health, explicit
`no_accounts` non-readiness, and clean SIGTERM. Its first invocation rejected
the missing checksum sidecar before installing or executing the artifact.

Only `codex-account-router.service` restarted, from PID 524721 on 0.2.5 to PID
548777 on 0.2.6. Twenty readiness samples returned HTTP 200. All standalone
8215 and routed 8216 Web/App Server PIDs, start-monotonic timestamps, releases,
and effective-unit digests remained unchanged. Tailnet 8215 and 8216 returned
HTTP 200, and the 8216 browser showed the signed-in composer after deployment.
No model request or account switch was sent for this live verification. See
`evidence/M6.9/router-r24-runtime-switch-summary.json`.

During R24 the user again delegated an 8215 startup-page report to a subagent.
Its read-only server checks passed, and a fresh browser page reached the
signed-in composer in one 14.65-second observation. No 8215 mutation or restart
was justified. The main thread's later router-only 0.2.6 activation preserved
both 8215 and 8216 Web/App Server invariants. See
`evidence/M6.9/standalone-8215-startup-r4-review.md`.

## R25: user-prioritized standalone 8215 startup diagnosis

The user paused the planned routed 8216 performance continuation and requested
that standalone 8215 startup latency be addressed first. The minimal experiment
separated HTTP delivery, static compression, browser startup, Web IPC, App
Server initialization, host firewall behavior, and the two-sided Tailscale path
before any deployment was considered.

The 8215 root returned HTTP 200 in 0.56 seconds. Web IPC remained about 4 ms and
App Server initialization plus account/model metadata reads remained about
44–46 ms. The immutable main module was already minified and Brotli quality 11
compressed from 14,138,835 bytes to 3,294,106 bytes, but its observed transfer
time varied from about 23 to 64 seconds. An identity request transferred only
8,809,027 bytes before its 120-second deadline.

Five Mac-to-server probes remained on `DERP(tok)` and ten reverse probes
remained on `DERP(hkg)`; neither direction established a direct connection. The
server already used fixed UDP port 41641, its host firewall allowed that port,
and both hosts reported working UDP. The server's local router reported a
private `192.168.33.0/24` WAN address, proving an upstream double-NAT boundary
that cannot be repaired by restarting codex-web or changing the Linux firewall.

The existing directly reachable VPS is the appropriate bounded peer-relay
candidate because it can preserve `http://100.95.50.98:8215/`. The current
thread does not have an explicitly authorized SSH identity for that separate
host, so it was not probed by guessing accounts and no Tailnet policy was
changed. No service, file, release, unit, credential, model request or account
selection changed during this diagnosis. See
`evidence/M6.9/standalone-8215-startup-r5-network-diagnosis.md`.

### R25 safe repair experiment

After the user explicitly authorized repair, a temporary nftables rule allowed
only root-owned UDP source port 41641 to bypass the host TUN policy. A transient
systemd rollback was scheduled before the mutation. The rule matched 90
Tailscale packets, but both directions remained DERP relayed, confirming that
the upstream double-NAT boundary remained authoritative. The rule was rejected
and rolled back early. The original persistent nftables file hash and every
production PID/start timestamp remained unchanged.

The inner TP-Link router had no UDP 41641 mapping, but all 16 UPnP slots were
occupied by unrelated `wechat voip` entries. A bounded mapping request failed
before creation and no existing entry was deleted. The upstream H3C management
page requires an interactive login; no credential guessing occurred.

A no-model country check under the `codex` service UID returned `JP`, preserving
the required Japan VPN egress. As a safe continuation, the current China Mac
was enabled as a reversible UDP 40000 peer-relay candidate while retaining an
empty exit-node selection. Its NAT is destination-independent and has both
UPnP and IPv6. A narrowly scoped Tailnet grant is still required, and the admin
page currently awaits interactive user sign-in. See
`evidence/M6.9/standalone-8215-startup-r6-safe-network-repair.md`.

### R26 peer-relay authorization result

After the user completed the interactive Tailnet administrator sign-in, one
narrow grant authorized only the restrictive 8215 server to use only the
current Mac as a Peer Relay. Existing network and SSH rules were preserved.
The server then listed the Mac as its candidate relay and both clients received
the expected relationship capabilities, proving that the policy direction and
control-plane propagation were correct.

The data-plane experiment did not complete a relay handshake. The Mac listener
was active on UDP 40000, but its relay session remained at zero packets with
one connecting and zero open endpoints. Both directions continued to use DERP.
A bounded UPnP diagnostic showed that the Mac is also behind two NAT layers:
the nearest gateway's external address is private and the outer gateway does
not expose a usable UPnP control endpoint. The diagnostic mapping was removed
and no persistent router mapping changed.

Current delivery was nevertheless substantially faster than the earlier
incident: three cache-busting 3,294,106-byte Brotli transfers completed in
3.257, 3.577, and 2.573 seconds, and one blank-document browser navigation
reached the signed-in composer in 6.282 seconds. These are bounded observations
over the still-DERP path, not a stable speed guarantee.

All five production PID/start baselines were unchanged, both server-local Web
roots returned HTTP 200, and the `codex` service UID still exited through
Japan. No model request or account switch was sent. Completing Peer Relay
requires an authorized UDP 40000 double-port-forward or an authorized directly
reachable VPS relay. See
`evidence/M6.9/standalone-8215-startup-r7-peer-relay-grant.md`.

## Blocker and residual risk

The real A-to-B acceptance step requires explicit authorization to enroll the
standalone-only second credential into the router. Fixtures prove the routing
policy but cannot prove provider behavior across two real router bindings. The
r20 preload immutable caching, r19 main-bundle reduction, r17
synchronous-request removal, r18 HTML precompression, r16 cold
resource-discovery ordering, r15 telemetry warning removal, and static
delivery boundary are qualified, while end-to-end startup and model latency
remain variable. R21 accepts the previously rejected exact HTTP/SSE title
transport path, but no post-R21 real title request was sent. R22 removes
repeated fresh model-catalog waits, but the first request after router restart
or TTL expiry still depends on the provider and the in-memory cache is not a
persistent availability mechanism. Full Access on 8215
deliberately removes Codex and outer systemd
filesystem/network sandboxing for the `codex` Unix account; trusted inputs and
normal host access controls are therefore required. R24 now connects the
production runtime and UI contracts, but the preference is process-local and
the current-route display resets until the first routed request after restart.
See
`evidence/M6.9/blocker.md`.
